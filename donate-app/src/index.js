import React from "react";
import ReactDOM from "react-dom";

const App = () => (
  <div className="text-lg text-magenta-500">Serverless React</div>
);

ReactDOM.render(<App />, document.getElementById("react-root"));
